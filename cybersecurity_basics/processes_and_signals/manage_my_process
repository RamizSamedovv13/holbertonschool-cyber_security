restart() {
    if [ -f "$FILE" ]; then
        PID=$(cat "$FILE")
        kill "$PID"
        rm -f "$FILE"
    fi

    bash ./manage_my_process &
    echo $! > "$FILE"

    echo "manage_my_process restarted"
}
